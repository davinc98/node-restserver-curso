const express = require('express');
const app = express();
const bcrypt = require('bcrypt');
const Usuario = require('../models/usuario');


//========================
//========================

module.exports = {
    app
};